name: repo-hygiene

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check tracked raw data and file sizes
        shell: pwsh
        run: ./scripts/check_repo_hygiene.ps1
