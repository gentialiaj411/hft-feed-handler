# CLAIMS_MATRIX.md

| Claim | Current evidence | Risk level | Missing proof | Suggested verification |
|---|---|---|---|---|
| 3-venue binary protocol parsing | Stated in README + passing `test_cboe_pitch_parser` | Medium | NASDAQ/IEX parser evidence still partially indirect; end-to-end replay not validated here | Inspect remaining parser tests/fuzz targets; run narrow parser tests |
| Deterministic merge with CRC invariance | Stated in README | High | No repeated-run evidence checked in this pass | Run determinism regression script and compare CRC outputs |
| Gap recovery with bounded buffering + force_advance | Stated in README | Medium | Behavior under edge loss patterns unverified | Run targeted recovery tests with controlled sequence gaps |
| A/B arbitration with drop injection | Stated in README | Medium | No empirical validation inspected | Run A/B evidence scripts and confirm CRC comparisons |
| Order book and NBBO venue consolidation | Source inspection + passing `test_phase3_order_book_nbbo` + `test_phase3_feature_bridge` | Medium | Broader scenario coverage still unverified | Extend with cancel/replace edge cases and cross-venue stress |
| NBBO + six feature engine outputs | Stated in README | Medium | Feature correctness has passing mirrored C++ regression evidence; benchmark claims updated from local WSL runs | Cross-check with `validate_feature_math.py`, `test_phase3_feature_pipeline`, and README benchmark numbers |
| Lock-free SPSC ring buffer | Stated in README | Medium | Concurrency correctness/perf unverified | Inspect ring buffer tests + run focused stress tests |
| Feature publication hot-path latency | `bench/results/feature_latency_wsl2_4b5ab45af37c4ea1fee77aea27e89ef8fa35ff5a.json` and `bench/results/feed_hot_path_wsl2_4b5ab45af37c4ea1fee77aea27e89ef8fa35ff5a.json` (Release, WSL2, 2M events, two runs each) | Medium | WSL2-only artifacts; bare-metal Linux comparison still TODO/VERIFY | Re-run on bare-metal Linux if you want tighter host-generalizable numbers |
| libFuzzer targets for 3 parsers | Stated in README | Low | Existence/execution status unverified | Locate fuzz targets and run short fuzz smoke |
| Multicast UDP ingestion w/ A/B failover (Phase B) | Passing `test_phase_b_framing`, `test_phase_b_udp_loopback`, `test_phase_b_ab_failover`, `test_phase_b_burst_loss` under `~/mdh-build/` (GCC 15.2.0, WSL2 Ubuntu) | Medium | End-to-end cross-process throughput/latency artifact under `bench/results/phase_b_wire_*.json` TODO/VERIFY due WSL2 multicast loopback limitations | Re-run smoke on bare-metal Linux or non-WSL Linux host |
| Journal format + replay determinism (Phase C) | Passing `test_phase_c_journal_roundtrip`, `test_phase_c_journal_torn_record`, `test_phase_c_replay_determinism`, and `test_phase_c_journal_format_version` under `~/mdh-build/` | Medium | Recorded-journal artifact and broader replay coverage still TODO/VERIFY | Run journal replay against a canonical recorded journal and compare hashes |
| Matching engine + market-making strategy + PnL (Phase A) | Passing `test_phase4_sim_matching_engine`, `test_phase4_market_making_strategy`, `test_phase4_pnl_accountant`, `test_phase4_backtest_runner` under `~/mdh-build/`; `journal_replay` on 5M recorded ITCH journal completed with `records_read=5000000`, `crc_failures=0`, `pipeline_crc_hex=0x1f27338a` | Medium | `journal_replay` Phase 4 backtest path currently produced zero fills/PnL on this slice; research runner has stronger fill/PnL evidence | Prefer research-run artifact for recorded strategy metrics, or improve `journal_replay` strategy path before using Phase 4 metrics externally |
| NUMA-local pinned SPSC publication with hugepage-aware backing (Phase D) | Code implementation added: `mf::os::{cpu_affinity,numa,hugepages}`, `include/mf/core/spsc_ring_aligned_storage.hpp`, `tools/phase_d_latency_bench.cpp`, `tests/test_phase_d_*` | High | Linux runtime evidence absent on this host; no baseline-vs-tuned artifact captured yet | Run Phase D build/tests/bench on Linux and capture `bench/results/phase_d_pin_*.json` + topology output |
| Event-sourced research simulation architecture | Added `mf::research::{EventStore,SimulationClock,StrategyEngine,ExperimentRunner}` with passing `test_research_event_store`, `test_research_simulation_clock`, `test_research_strategy_engine`, and `test_research_experiment_runner` under `~/mdh-build/`; synthetic 2M artifacts `bench/results/experiment_synth2m_{a,b}.json` match input CRC `2142301960`, config hash `2743174372`, output hash `82487151`; recorded 5M ITCH artifacts `bench/results/experiment_itch_5m_{a,b}.json` match input CRC `2369313900`, config hash `2743174372`, output hash `882212532`, records `5000000`, orders `121950`, fills `1387`, clock rejects `0`, CRC failures `0` | Low | Recorded artifact is bounded to first 5M events of one NASDAQ ITCH day under WSL2; not a cross-venue recorded artifact | If desired, run larger/full-day and add IEX/Cboe recorded artifacts |
| Raw ITCH -> canonical journal converter (NASDAQ) | Added `tools/itch_to_journal.cpp` + CMake wiring; converter emits summary counters and writes via existing `JournalWriter` API; `journal_replay` accepts legitimate post-`Delete` ITCH event types; `test_phase_c_mixed_event_replay` and `test_phase2_recovery_pending_dedupe` pass on WSL; 5M real ITCH conversion/replay completed with `events_written=5000000`, `unhandled_msg_count=211118`, `parser_malformed_messages=0`, `records_read=5000000`, `crc_failures=0`, `merged_crc=0x1f27338a` | Low | Converter is NASDAQ-only and bounded artifact is 5M events, not full-day | Run full-day conversion/replay if full-tape claim is needed |

Update 2026-05-18: Phase D implementation landed, but Phase D runtime benchmark validation is still TODO/VERIFY.

Update 2026-05-18: Research architecture layer, deterministic synthetic-journal test, and 2M synthetic canonical-event experiment artifacts pass under WSL `~/mdh-build/`; large recorded market-tape experiment artifact remains TODO/VERIFY.

Update 2026-05-18: Task 2 gate check confirms no existing raw NASDAQ ITCH -> canonical journal conversion tool in this repo, and `phase4_backtest` currently runs only synthetic in-memory events; recorded-tape Phase 4 artifact remains TODO/VERIFY pending a minimal adapter.

Update 2026-05-18: Minimal NASDAQ raw-ITCH converter was added, but Task 2 remains blocked because `journal_replay` does not yet successfully process the converted output in this environment; no Phase 4 recorded-tape artifact was produced.

Update 2026-05-18: Diagnosis run confirmed `journal_replay` succeeds on a known-good synthetic journal generated by `make_research_journal` (exit 0), but exits 1 silently on a tiny ITCH-converted journal (`--max-events 1000`); WSL service instability (`no installed distributions`, `Wsl/Service/E_UNEXPECTED`) intermittently interrupts shell/trace commands, so root-cause isolation remains TODO/VERIFY until environment stability is restored.

Update 2026-05-19: Replay implementation patch removed the `journal_replay` hard-fail for event types above `Delete`, because real ITCH journals can contain `Replace`, `Trade`, `CrossTrade`, `Imbalance`, `System`, and `StockDirectory`. Added mixed-event Phase C and recovery pending-dedupe regressions, made `itch_to_journal` overwrite output by default unless `--append` is passed, and preserved unknown ITCH messages as `Unknown` placeholders to avoid artificial sequence gaps. Local Windows CTest validation passed for parser/phase2/phase3/phase4/research targets. WSL tiny real ITCH conversion/replay now succeeds for 1000 events; larger recorded-tape artifacts remain TODO/VERIFY.

Update 2026-05-19: WSL 5M recorded ITCH validation succeeded. `itch_to_journal` converted 5,000,000 messages/events from the 2019-01-30 NASDAQ ITCH tape with `parser_malformed_messages=0`; `journal_replay` completed with `records_read=5000000`, `crc_failures=0`, `pipeline_crc_hex=0x1f27338a`; `experiment_runner` completed twice with matching input CRC `2369313900`, config hash `2743174372`, output hash `882212532`, orders `121950`, fills `1387`, Sharpe `0.107187`, fill ratio `0.0113735`, and max drawdown `9325000`.
